# Architecture

담당: B 트랙. S4에 확정 예정이며, 현재는 목차만 있습니다.

작성 예정 목차:

1. 문제 정의 — 대기인원은 왜 실측되지 않는가
2. 대기인원의 3요소 (신규 도착 / 잔류 / 환승 대기)
3. 큐 수지(queue balance) 방정식
4. 순환검증을 탈출하는 물리 제약
5. 선행 프로토타입과의 차별성 (목적·핵심기능·기술구조·핵심알고리즘 비교)
6. 시스템 아키텍처 다이어그램
7. API 계약

> **메모 (C, issue #57 작업 중 정리, 8/14)** — §7 작성 시 참고: `/congestion`·`/timeline`은
> 지금은 과거 한 달치 데이터를 시간대(0~23시)별로 정규화한 값만 반환하지만, 나중에 실시간
> 데이터로 바꿀 여지를 남기도록 설계했다. `hour` 파라미터를 명시하면 항상 "그 과거
> 평균 패턴"을, 생략하면 "현재값"을 반환한다 — 지금은 "현재값"이 `_current_hour()`로 과거
> 평균 테이블에 매핑되지만, 실시간 데이터로 전환 시엔 이 "현재값 조회" 로직만 실시간 저장소 조회로
> 바꾸면 된다. 즉 실시간 전환 비용은 `ingest/`·`features/`(재캘리브레이션)에만 들고, API 계약 자체는 안정적으로 유지된다.
