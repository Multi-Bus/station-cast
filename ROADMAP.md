# Roadmap

MVP(2026 오픈소스개발자대회 출품 범위) 이후 방향을 정리한다.

## 1. 실시간 연동

지금은 `estimator/wait_population.py`가 OA-12913(2026년 6월 1개월치)이라는 고정된 과거
파일을 읽어 W(s,t)를 계산하고, `/congestion`·`/corridor`가 `hour`를 생략하면 그 값을
"현재값"으로 반환한다. 이 "현재값" 조회 로직(`_current_hour()`)은 처음부터 실시간
데이터로 바꿔치기할 수 있게 설계돼 있다 — `hour`를 명시한 요청(과거 평균 패턴)과
생략한 요청(현재값)의 API 응답 형태 자체는 그대로 두고, 서버 내부에서 "현재값을
어디서 가져오는지"만 실시간 소스로 교체하면 된다(`ARCHITECTURE.md` §6).

같은 코드베이스 안에 이미 실시간 연동의 선례가 있다 — `/stops/{id}/arrivals`는
서울시 TOPIS를 매 요청마다 직접 호출하는 실시간 기능이다(`ingest/realtime_arrival.py`,
issue #48). W(s,t)를 실시간화하려면 이 패턴을 승하차 집계에도 적용해야 하는데,
현재 서울 열린데이터광장은 시간대별 승하차를 실시간으로 제공하지 않아서 그런 소스
자체가 아직 없다. 그래서 이 방향의 실제 작업은 API를 뜯어고치는 일이 아니라
실시간 승하차 소스를 확보하고 `ingest/`·`features/`(재캘리브레이션 주기 결정 포함)를
새로 만드는 일이 된다.

## 2. 타 도시 확장

지금 파이프라인은 서울 중심부 21개 정류장(종로·명동·을지로 zone)과 서울시 전용
공개 데이터(OA-12913/12912, 서울시버스노선기본정보, TOPIS)에 맞춰져 있다. 확장 시
바뀌는 부분과 안 바뀌는 부분이 나뉜다:

- **안 바뀌는 부분**: `estimator/wait_population.py`의 Little's Law 계산 자체는
  도시에 무관하다 — 어떤 도시든 노선별 승차(`B_r(s,t)`)와 배차간격만 있으면 같은
  공식이 그대로 적용된다.
- **바뀌는 부분**: `ingest/`의 각 모듈은 서울 열린데이터광장의 특정 파일 형식·API에
  맞춰 작성돼 있어, 다른 도시로 가면 그 도시의 공개 데이터 포맷에 맞는 어댑터를
  새로 짜야 한다. 회랑(정류장 21개) 선정 기준도 그 도시 데이터로 다시 밟아야 하고,
  버스 정원 가정과 정류장 포용인원도 현장 실사나 해당 도시 자료로 다시 잡아야 한다.

국내 다른 지자체도 공공데이터포털을 통해 유사한 구조(노선×정류장×시간대 승하차)의
데이터를 공개하는 경우가 많아, `ingest/` 계층만 새로 짜면 나머지(estimator·validate·
api)는 그대로 재사용 가능한 구조로 설계돼 있다.

## 3. 운수업체 배차 분석

`validate/physical_constraints.py`의 `capacity_violation_report()`는 이미 (정류장,
노선, 시간대)마다 실측 승차가 그 시간대 이론적 최대 수송능력(배차간격 기준 최대
운행 가능 인원)을 넘는지를 계산하고 있다 — 지금은 QA용 CSV로만 내보내는 내부
검증 산출물이지만, 이 데이터 자체가 이미 "어느 노선·정류장·시간대가 만성적으로
배차 부족인가"를 보여준다. 운수업체 입장에서는 이걸 "이 시간대엔 배차를 늘려야
한다"는 의사결정 자료로 쓸 수 있다.

로드맵상 남은 일은 계산 로직 자체가 아니라 **노출 방식**이다: 지금은 정류장 단위
승객용 화면(`/congestion`, `/timeline`)만 있고, 노선 단위로 위반율을 집계해서 심각도
순으로 보여주는 운수업체용 뷰(또는 API 엔드포인트)가 없다. 이걸 추가하면 같은
검증 파이프라인이 승객용 서비스와 운수업체용 분석 도구 양쪽에 쓰이게 된다.

## 4. 커뮤니티 확장 가능성

- **데이터 접근성**: 모든 원본 데이터가 서울 열린데이터광장·공공데이터포털의
  공개 데이터다. 대부분 인증키가 필요 없거나(OA-12913/12912, 버스정류소 위치,
  버스노선 정보), 필요해도 자동승인이라 즉시 발급된다(TOPIS, 특일정보) —
  유료 계약이나 승인 대기 없이 외부 기여자가 바로 데이터를 내려받아
  `scripts/build_processed.py` 한 번으로 전체 파이프라인을 재현할 수 있다.
- **계층 분리**: `ingest → features → estimator → validate → api → frontend`로
  역할이 뚜렷하게 나뉘어 있어, 기여자가 전체 구조를
  몰라도 한 계층만 이해하고 기여할 수 있다 — 예를 들어 타 도시 확장은
  `ingest/`에 새 어댑터를 추가하는 작업으로 국한된다.
- **라이선스 정책 명문화**: 새 의존성은 OSI 승인 permissive 라이선스만 허용하고
  GPL·AGPL·LGPL 계열은 기본 차단한다는 기준이 `docs/LICENSE_POLICY.md`에 문서화돼
  있어, 외부 기여자가 라이선스 충돌을 걱정하지 않고 의존성을 추가할 수 있다.
- **기여 절차 문서화**: 브랜치명·커밋 컨벤션·PR 절차·CI 게이트(ruff·mypy·pytest·
  라이선스 스캔)가 `CONTRIBUTING.md`에 명시돼 있고, 로컬 개발 환경도 Python 3.11에서
  `pip install -c constraints.txt -e ".[dev]"` 한 줄로 끝난다 — 새 기여자가 코드
  스타일이나 워크플로를 추측할 필요가 없다.
